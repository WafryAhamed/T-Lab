[PHP]
; Keep this configuration portable for local development and cloud deployments.
; Railway/Linux builds load extensions from the runtime's default extension
; directory. Do not hard-code Windows-specific extension paths here.

